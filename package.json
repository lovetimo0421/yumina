{
  "name": "yumina",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @yumina/server db:generate",
    "db:migrate": "pnpm --filter @yumina/server db:migrate",
    "db:push": "pnpm --filter @yumina/server db:push",
    "db:studio": "pnpm --filter @yumina/server db:studio"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
